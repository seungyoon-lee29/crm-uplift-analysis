# 아마존 셀러 카테고리 진입 분석 — 아키텍처 & 파이프라인 설계서

> 작성일: 2026-06-12 · v1.2 (2026-06-19 2·3차 리뷰 반영)
> 프로젝트: "전직 아마존 셀러가 데이터로 다시 푸는 카테고리 진입 의사결정"
> 구성: 설계(1~6장) → 비판적 리뷰(7장) → 리뷰 반영 최종 결정사항(8장) → 2차 리뷰 D1~D10(9장) → 3차 리뷰 E1~E12(10장)
> v1.1 변경 요약: repo 경계 명문화(§9.1), Q3 타깃 누수 차단·단순화(§3.3·§5), 지표 산식 확정(§3·§9.2), MVP 최소 단위 추가(§8.5), LLM 재현성·데이터 버전 고정(§4·§9.3) 등 D1~D10 반영.
> v1.2 변경 요약: 데이터 스키마/실행 단계 결함 반영 — 분석 단위=parent_asin(E1), 컷오프 우편절단·코호트 성숙도(E2·E4), price 결측 게이트(E3), 평가지표 정합(E6), 배포 데이터 동봉(E7), 환경 재현성·누수 가드 테스트(E8·E9) 등 E1~E12. 상세 §10.

---

## 1. 분석 질문 정의 (모든 설계의 기준점)

**메인 질문**: 신규 셀러 관점에서, 어떤 니치(세부 카테고리)가 진입 가치가 있으며, 어떤 초기 조건(가격대, 차별화 포인트)으로 진입해야 하는가?

서브 질문 4개로 분해:

| # | 서브 질문 | 산출물 |
|---|---|---|
| Q1 | 니치별 경쟁 강도와 수요는 어떻게 다른가? | 니치 기회 스코어카드 |
| Q2 | 저평점 리뷰에 반복되는 미충족 니즈는 무엇인가? | 니치별 unmet needs 리포트 |
| Q3 | 출시 후 1년 내 "안착"하는 상품의 초기 조건은? | 성공 예측 모델 + 피처 해석 |
| Q4 | (선택) 진입 후 가격·경쟁 환경은 어떻게 전개되는가? | Keepa 시계열 사례 분석 |

> 원칙: 모든 분석 단계는 위 4개 질문 중 하나에 매핑되어야 함. 매핑 안 되는 분석은 만들지 않는다(스코프 크리프 방지).

---

## 2. 전체 아키텍처

```
[데이터 소스]                [수집/Ingestion]         [저장 Storage]
HuggingFace                  Python datasets lib       data/raw/        (parquet, 불변)
Amazon Reviews 2023  ──────► 카테고리별 스트리밍   ──►
(리뷰 + 메타)                다운로드 스크립트          data/staging/    (정제·표준화)
                                                            │
Keepa API (선택, Q4) ──────► keepa python client  ──►  data/marts/     (분석용 테이블)
                                                            │
                             ┌──────────────────────────────┘
                             ▼
[변환/분석 Processing]                          [서빙 Serving]
DuckDB + Polars (SQL/집계)                      Streamlit 대시보드 (니치 탐색기)
Jupyter (EDA, Q1)                               최종 리포트 (의사결정 메모 형식)
sentence-transformers/LLM (Q2 텍스트마이닝)      GitHub README (스토리텔링)
LightGBM + scikit-learn (Q3 모델)
                             │
[재현성 Reproducibility]
Makefile (make ingest / transform / train / dashboard)
requirements.txt + 시드 고정 + data dictionary
데이터셋 버전 핀(HF revision/체크섬) + LLM 출력 스냅샷 캐싱 (v1.1, D5·D9)
```

기술 스택 선정 원칙: **분석가 포트폴리오이지 엔지니어링 포트폴리오가 아니다.** Airflow·Spark·클라우드는 의도적으로 배제. 대신 SQL 역량 증명을 위해 DuckDB를 변환 레이어에 명시적으로 사용(채용공고의 SQL 요구 대응).

## 3. 데이터 레이어 설계

### 3.1 소스 및 범위 확정

| 소스 | 용도 | 범위 |
|---|---|---|
| Amazon Reviews 2023 — 리뷰 | Q1, Q2, Q3 | 카테고리 2개로 한정 (아래 기준) |
| Amazon Reviews 2023 — item metadata | Q1, Q3 | 동일 카테고리 |
| Keepa API | Q4 | 최종 후보 니치의 ASIN 100~300개, 1개월 구독 |
| 수기 가정 테이블 | 마진 시뮬레이션 | FBA 수수료율, 물류비 가정 (출처 명시) |

**카테고리 선정 기준**: ① 본인이 셀러 시절 실제 취급했던 카테고리 1개(서사의 핵심) + ② 대조군으로 경쟁 구조가 다른 카테고리 1개. 3개 이상 금지 — 용량과 분석 깊이 모두 망가짐.

**분석 기간**: 2015-01 ~ 2023-09. 1996년부터 받지 않는다(초기 데이터는 희소하고 현재 마켓플레이스 구조와 무관).

### 3.2 저장 구조 (3-레이어)

```
data/
├── raw/        # 소스 그대로. 절대 수정 금지 (parquet, 카테고리/연도 파티션)
├── staging/    # 타입 정리, 중복 제거, 필터링. 1소스=1테이블 원칙
└── marts/      # 분석 단위 테이블 (DuckDB SQL로 생성)
    ├── dim_product        # parent_asin(상품) 단위: 가격, 브랜드, 니치 라벨, 첫 리뷰일
    ├── fct_review         # 리뷰 단위: 평점, 텍스트, verified 여부 (asin·parent_asin 모두 보존)
    ├── mart_niche_monthly # 니치×월: 리뷰 수, 신규 상품 수, 평점 분포 → Q1
    └── mart_launch        # 상품(parent_asin)×출시 코호트: 초기 조건 + 1년 후 결과 → Q3
```

> **분석 단위 = `parent_asin`(상품) (v1.2, E1)**: Amazon Reviews 2023은 리뷰에 `asin`(변형: 색상·사이즈)과 `parent_asin`(상품)이 따로 있고 **메타데이터는 `parent_asin` 기준**. "상품 수", 출시일, HHI, 코호트는 모두 `parent_asin`으로 집계한다(변형을 상품으로 세면 상품 수·경쟁 집중도·출시일이 왜곡됨). 리뷰는 `parent_asin`로 롤업하되 `asin`도 보존.

### 3.3 핵심 정의 (데이터 사전에 명문화)

- **니치(niche)**: 메타데이터 category 트리의 leaf 노드 기준. 단, 상품 수 30개 미만 leaf는 부모로 합침
- **출시일(launch date)**: 메타데이터에 출시일 없음 → **첫 리뷰 날짜로 프록시** (한계 7장에서 검토)
- **수요 프록시**: 월별 신규 리뷰 수 (리뷰≠판매 문제는 7장)
- **성공(Q3 타깃)** *(v1.1 재정의, D2)*: 첫 리뷰 후 12개월 누적 리뷰가 **동일 니치·동일 출시 코호트 내 상위 분위**(기본 상위 40%)에 드는가 — 단일 축의 상대 기준.
  - 임계값은 **출시 시점까지 이미 출시된 코호트 분포로 point-in-time 계산**(미래 정보로 임계값을 만들지 않음 → §7 R4 누수 차단).
  - "중앙값(상위 50%)"은 정의상 절반이 성공이 되는 자기충족을 피하기 위해 사용하지 않고, 상위 분위로 비대칭 설정.
  - 평균 평점 ≥ 4.0은 타깃과 AND로 결합하지 않고 **보조 품질 필터/세그먼트 변수**로 분리(평점 분포를 타깃에 섞지 않음).
  - **코호트 성숙도 제약 (v1.2, E2)**: 12개월 타깃은 데이터 컷오프(2023-09)에서 우편절단된다. **출시일 ≤ 2022-09 코호트만** 완전한 12개월 윈도우를 가지므로 Q3 모집단을 거기로 한정한다(2022-10 이후 출시분은 타깃 미성숙 → 제외). 이 제약이 Stage 5 시간 split·표본 크기에 직접 영향.

## 4. 파이프라인 단계별 설계

### Stage 0 — 환경/스캐폴딩 (0.5주)
repo 구조, Makefile, requirements, 데이터 사전 템플릿. 노트북은 `01_`, `02_` 번호 체계.
- **환경 재현성 (v1.2, E8)**: `requirements.txt`는 **버전 핀(가능하면 해시 락)** + Python 버전 고정(`.python-version`/`pyproject`). 데이터뿐 아니라 환경도 재현 가능해야 함(D9 보완).
- **누수 가드 테스트 (v1.2, E9)**: point-in-time·임계값 누수 방지를 수동 체크리스트에만 의존하지 않고, **소수의 자동 단위 테스트**(예: 피처 계산 시점이 출시일을 넘지 않는지, 타깃 임계값이 출시 시점 코호트로만 계산되는지)를 `tests/`에 둠 → 규율 + 엔지니어링 어필.

### Stage 1 — Ingestion (1주)
- HuggingFace `datasets` 라이브러리로 카테고리별 스트리밍 다운로드 → 연도별 parquet 저장
  - **수집 메모 (v1.2)**: 이 데이터셋은 커스텀 로딩 스크립트 기반이라 HF 데이터셋 뷰어/parquet export가 동작하지 않음(501). 반드시 `datasets` 로딩 경로로만 접근. 리뷰↔메타 조인은 **`parent_asin` 키**로 수행(E1).
- 수집 직후 검증 스크립트: 행 수, null 비율, 기간 커버리지, ASIN 중복률 기록 → `data_quality_report.md` 자동 생성 (데이터 품질 검증 습관 자체가 어필 포인트)

### Stage 2 — Staging 변환 (1주)
- DuckDB SQL: 타입 캐스팅, 가격 파싱($ 문자열 → 숫자, 결측 처리), category 트리 → 니치 라벨 정규화
- 중복 리뷰 제거, unverified 리뷰 플래그
- **price 커버리지 게이트 (v1.2, E3)**: 가격은 Q1 가격 포지셔닝·Q3 핵심 피처이나 메타 `price`는 결측·비표준($ 범위/문자열)이 많음. Stage 2에서 **니치별 price 커버리지율을 측정**하고, 임계(예: 60%) 미달 시 대체전략(브랜드/니치 중앙값 대치) 또는 가격 분석 범위 축소를 결정 — 핵심 변수가 대량 결측이면 분석이 붕괴하므로 사전 게이트로 차단.
- **여기서 가장 시간이 오래 걸림을 전제로 일정 배분** (실데이터의 메타데이터는 지저분함)

### Stage 3 — Q1: 니치 기회 스코어링 (1.5주)
- mart_niche_monthly 기반 지표. **산식 확정 (v1.1, D3)** — 상세는 §9.2 표:
  - 수요 성장률: 니치 월 신규 리뷰 수의 최근 12개월 대비 직전 12개월 증가율(롤링)
  - 경쟁 집중도: HHI = Σ(상품별 니치 내 리뷰 점유율)². 보조로 상위 5개 상품 리뷰 점유율
  - 신규 진입 속도: 니치 월별 신규 상품(첫 리뷰 발생 ASIN) 수의 추세
  - 평점 갭: 니치 평균 평점의 전체 니치 분포 대비 위치(낮을수록 품질 개선 진입 기회)
- 각 지표 **z-score 표준화 후 가중합 → 기회 점수**. **가중치는 민감도 분석 필수**(가중치 바꿔도 상위 니치 유지되는지)
- **컷오프 경계 보정 (v1.2, E4)**: "최근 12개월 ÷ 직전 12개월" 성장률·신규 진입 속도는 2023-09에서 윈도우가 잘려 최근값이 체계적으로 하향(신생 니치 과소평가). 경계 구간을 별도 표기하거나 성장률 기준월을 컷오프에서 일정 마진 당겨 계산.
- **니치 수 가드 (v1.2, E5)**: 카테고리 2개 하드캡으로 leaf 니치 수가 적으면 "전체 니치 분포 대비 z-score/백분위"가 불안정. 니치 수가 적을 경우(예: <20) 표준화를 카테고리 통합 분포로 하거나 점수 해석을 순위 중심으로 제한.
- **외부 검증 (v1.1, D6)**: 기회 점수가 합성 지표인 만큼 독립 신호와 상관 점검 — 가능 시 Keepa BSR 성장(Q4), 불가 시 차기 12개월에 실제 실현된 리뷰 성장과의 상관으로 점수 타당성 보강(순환 검증 방지)
- 본인이 팔았던 니치의 점수를 회고 검증: "당시 이 분석이 있었다면 진입했을까?"

### Stage 4 — Q2: 미충족 니즈 텍스트 마이닝 (1.5주)
- 상위 기회 니치 3~5개의 1~3점 리뷰만 추출
- 임베딩(sentence-transformers) → 클러스터링(HDBSCAN) → 클러스터별 대표 리뷰를 LLM으로 라벨링/요약
- 산출: 니치별 "불만 토픽 × 빈도 × 예시" 테이블 → 차별화 진입 포인트 도출
- **재현성 (v1.1, D5)**: LLM 라벨링은 비결정적이므로 모델 버전·프롬프트를 고정하고 **출력 결과를 스냅샷으로 repo에 캐싱**(예: `data/marts/llm_labels.parquet` 대신 추적 가능한 산출물로 커밋)해 파이프라인 재실행 시 동일 결과 보장. 한계는 limitations.md에 명시
- 검증: 클러스터 라벨을 본인이 직접 샘플 50건 읽고 정합성 확인 (LLM 출력 맹신 금지)

### Stage 5 — Q3: 안착 예측 모델 (1.5주)
- 단위: mart_launch (출시 코호트)
- 타깃: §3.3 재정의대로 **동일 코호트 내 12개월 누적 리뷰 상위 분위(상위 40%)** 단일 축. 평점은 보조 세그먼트로만 사용
- 피처: **출시 후 첫 90일 내 정보만** (가격, 니치 경쟁 지표, 초기 평점, 초기 리뷰 텍스트 특성) — 누수 방지의 핵심
- **누수 체크리스트 (v1.1, D2)**: (1) 모든 피처가 "출시 시점에 알 수 있었는가" 통과, (2) 니치 지표는 point-in-time join, (3) **타깃 임계값(상위 분위)도 출시 시점까지의 코호트 분포로만 계산** — 임계값 누수 금지
- **코호트 성숙도 제약 (v1.2, E2)**: 12개월 타깃이 성숙한 **출시일 ≤ 2022-09 코호트만** 사용. 시간 split도 이 안에서 구성(예: ~2021 출시분 학습 / 2022-01~09 출시분 검증). 2022-10 이후 출시분은 타깃 미성숙으로 제외.
- 분할: **시간 기반 split** — 랜덤 split 금지
- **표본 크기 fallback (v1.2, E10)**: 단일 카테고리 × 성숙 코호트 × 니치 필터 후 N이 작을 수 있음. N이 작으면 LightGBM/SHAP 과적합 위험 → **로지스틱 회귀(규제) 베이스라인을 주 모델로** 두고 LightGBM은 보조. 표본 수를 리포트에 명시.
- 모델: 로지스틱 회귀(베이스라인, 해석) → LightGBM → SHAP으로 피처 기여 해석
- 평가 **(v1.2, E6)**: D2 재정의로 양성 클래스가 ~40%라 심한 불균형이 아님 → **ROC-AUC를 주지표로, PR-AUC를 병기**(불균형 강조 서술 갱신). 그리고 "이 모델로 진입 결정 시 기대 효용" 형태의 비즈니스 해석 한 단락.

### Stage 6 — Q4(선택): Keepa 사례 분석 (1주, 예산 시)
- Stage 3 상위 니치의 ASIN 100~300개 가격·BSR 히스토리 수집
- **토큰 쿼터 사전 산정 (v1.2, E12)**: Keepa는 토큰 쿼터제. €19 1개월 구독의 토큰 한도로 100~300 ASIN × 장기 히스토리가 초과될 수 있으므로 ASIN 수·히스토리 깊이를 사전 산정해 범위 조정.
- "성공/실패 상품의 진입 후 18개월 가격 전개" 사례 내러티브 2~3개

### Stage 7 — 서빙/패키징 (1.5주)
- Streamlit: 니치 탐색기(필터 + 스코어카드 + 불만 토픽)
- **배포 데이터 동봉 (v1.2, E7)**: `.gitignore`가 `data/`를 제외하므로 Streamlit Community Cloud 배포 시 앱이 읽을 marts가 없음. marts는 작은 집계 산출물이니 **배포용 경량 스냅샷(예: `app/data/*.parquet`)을 별도로 커밋**해 viewer가 즉시 실행되게 함(원천 대용량 데이터는 계속 제외).
- 최종 리포트: **"투자 의사결정 메모" 형식** (요약 → 추천 니치 → 근거 → 리스크 → 한계). 노트북 나열이 아니라 경영진 보고서처럼
- **PII 처리 (v1.2, E11)**: 리포트·대시보드에 리뷰 원문을 인용할 때 작성자명 등 식별 정보는 제거/익명화.
- README: 셀러 시절 일화로 시작하는 스토리텔링

**총 일정: 약 8~9주** (주 15~20시간 기준)

## 5. repo 구조

```
amazon-seller-entry-analysis/
├── README.md                  # 스토리 + 핵심 결과 3개 + 아키텍처 다이어그램
├── Makefile                   # ingest / transform / analyze / train / app
├── requirements.txt
├── docs/
│   ├── data_dictionary.md     # 모든 지표·용어 정의
│   ├── decisions.md           # 설계 의사결정 기록 (ADR 경량판) ★차별화
│   └── limitations.md         # 한계와 그 영향 명시 ★차별화
├── src/
│   ├── ingest/  ├── transform/ (SQL)  ├── features/  ├── models/
├── notebooks/                 # 01_eda ~ 05_model_interpretation
├── app/                       # streamlit
└── report/final_report.pdf    # 의사결정 메모
```

`decisions.md`와 `limitations.md`는 신입 포트폴리오에서 거의 못 보는 요소 — "판단 과정을 기록하는 분석가"라는 인상을 줌.

---

## 6. 데이터 수집에서 Playwright의 위치

Playwright MCP로 아마존을 직접 크롤링하는 것은 **배제 권장**. 이유: ① 아마존 ToS 위반 — 포트폴리오 공개 시 면접에서 컴플라이언스 감점 리스크(특히 핀테크), ② 안티봇으로 안정 수집 자체가 어려움, ③ 허깅페이스 데이터로 충분. Playwright를 쓰고 싶다면 ToS상 허용되는 보조 소스(예: 공개 통계 페이지) 정도로 한정하고, 그마저도 핵심 파이프라인에 의존성으로 넣지 말 것.

---

## 7. 비판적 셀프 리뷰 (Red Team)

설계를 채용 담당자/시니어 분석가 관점에서 공격해본 결과. **심각도 순.**

### R1. 리뷰 ≠ 판매 (심각도: 높음 / 회피 불가, 관리 대상)
리뷰 수를 수요 프록시로 쓰지만 리뷰 전환율은 카테고리·가격대·시기별로 다름(통상 구매의 1~2%). 저가 소모품은 리뷰율이 낮고, 리뷰 요청 정책 변화(2016 인센티브 리뷰 금지 등)로 시계열 단절도 있음.
**대응**: ① limitations.md에 1순위로 명시, ② 니치 "간" 절대 비교가 아니라 동일 니치 "내" 상대 비교와 추세 중심으로 해석 프레임 제한, ③ Q4 Keepa BSR로 일부 니치에서 리뷰-랭킹 상관을 교차 검증. 면접에서 반드시 물어볼 질문이므로 오히려 준비된 답변 기회로 전환.

### R2. 생존 편향 (심각도: 높음)
데이터셋에는 리뷰가 1개라도 달린 상품만 존재. **진입했다가 리뷰 0개로 사라진 상품(진짜 실패)은 보이지 않음.** Q3 "성공 예측"은 실제로는 "리뷰를 받은 상품 중 안착 vs 부진" 예측임.
**대응**: 타깃 정의를 정직하게 재명명("시장 안착" vs "조기 정체"), 리포트에 모집단 정의를 명시. 이걸 숨기면 면접에서 무너지고, 먼저 밝히면 신뢰를 얻음.

### R3. 출시일 프록시 오차 (심각도: 중간)
첫 리뷰일 = 출시일 가정은 리뷰가 늦게 달리는 상품에서 체계적으로 어긋남. 특히 Q3의 "첫 90일 피처"가 실제로는 출시 6개월 후일 수 있음.
**대응**: 메타데이터에 date 필드가 있는 상품 서브셋으로 프록시 오차 분포를 직접 측정해 부록에 수록. 오차가 크면 "첫 리뷰 후 90일"로 프레임 자체를 정직하게 변경.

### R4. Q3 데이터 누수 위험 (심각도: 중간 — 설계로 차단됨, 실행 규율 필요)
"12개월 내 리뷰 N개" 타깃에 "초기 리뷰 증가 속도" 피처를 넣으면 타깃을 피처로 예측하는 자기참조. 또한 니치 경쟁 지표를 전체 기간으로 계산하면 미래 정보 누수.
**대응**: 모든 피처는 "출시 시점에 알 수 있었는가" 테스트 통과 필수. 니치 지표는 출시 시점까지의 데이터로만 계산(point-in-time join). 코드 리뷰 체크리스트화. *(v1.1 D2: 타깃 임계값도 point-in-time 코호트 분포로 계산해 임계값 누수까지 차단 — §3.3·Stage 5 반영.)*

### R3×R4 결합 효과 (심각도: 중간 — v1.1 추가, D7)
R3(출시일 프록시 오차)와 R4(누수)는 독립 사건이 아니다. 첫 리뷰일=출시일 가정이 어긋나면 "출시 후 첫 90일 피처 윈도우"와 "12개월 타깃 윈도우"가 **동시에** 같은 방향으로 이동해 오차가 중첩된다(피처가 실제로는 출시 6개월 후 구간을 보거나, 타깃 12개월이 실제 18개월을 포함).
**대응**: R3에서 측정한 프록시 오차 분포를 두 윈도우에 **함께** 반영 — 오차가 큰 상품은 민감도 분석에서 제외하거나 별도 세그먼트로 보고. 단일 윈도우만 보정하고 다른 쪽을 방치하지 않는다.

### R5. 2023년 9월 컷오프 (심각도: 중간)
"2026년에 2023년 데이터로 진입 추천?"이라는 공격 가능. 특히 아마존은 2024~25년 중국 직발송(Temu류) 경쟁으로 구조가 변함.
**대응**: 리포트 프레이밍을 "지금 이 니치에 들어가라"가 아니라 **"진입 의사결정 프레임워크 구축 + 과거 데이터 검증"**으로 설정. Q4 Keepa(현재 데이터)가 최신성 보강. 이 프레이밍이면 컷오프는 약점이 아니라 범위 설정이 됨.

### R6. 니치 = category leaf 가정 (심각도: 중간)
아마존 카테고리 트리는 지저분함(중복 분류, 판매자 임의 등록, 깊이 불균일). leaf 기준 니치가 실제 시장 단위와 어긋날 수 있음.
**대응**: Stage 2에서 니치 라벨 품질 검사(니치당 상품 수 분포, 수동 샘플 검수 30개). 필요시 제목 임베딩 클러스터링으로 니치 재정의 — 단, 이건 일정 +1주 리스크이므로 품질이 심각할 때만.

### R7. 일정 낙관 편향 (심각도: 중간)
8~9주는 모든 단계가 순항할 때 기준. 경험상 Stage 2(정제)와 Stage 4(LLM 라벨링 검수)가 1.5~2배 걸림.
**대응**: **MVP 컷 정의** — Q1+Q2+리포트만으로도 제출 가능한 포트폴리오가 되도록 단계를 독립적으로 완결. Q3는 확장, Q4는 보너스. "다 못 끝내면 전부 미완성"인 구조 금지.

### R8. 용량/컴퓨팅 (심각도: 낮음~중간)
카테고리에 따라 리뷰 수천만 건 → 로컬 메모리 초과 가능.
**대응**: 스트리밍 다운로드 + 연도 파티션 parquet + DuckDB(out-of-core) 조합이면 노트북에서 처리 가능. 임베딩은 저평점 리뷰 서브셋(수십만 건)에만 적용. 그래도 크면 니치 샘플링.

### R9. "LLM 사용"이 오히려 흔해 보일 위험 (심각도: 낮음)
2026년 기준 "리뷰를 LLM으로 요약"은 더 이상 차별점이 아님.
**대응**: LLM은 도구로만, 차별점은 **검증 설계**(수동 샘플 정합성 확인, 클러스터 안정성)와 **의사결정 연결**에 둠. README에서 LLM을 전면에 내세우지 않음.

### R10. 대시보드 스코프 크리프 (심각도: 낮음)
Streamlit에 기능을 계속 붙이다 분석 깊이가 희생되는 전형적 함정.
**대응**: 화면 3개(니치 스코어카드 / 니치 상세 / 불만 토픽)로 고정. 그 이상 금지.

### 리뷰 총평
설계의 최대 약점은 기술이 아니라 **프록시의 정직성(R1~R3)**이다. 세 가지 모두 회피 불가능하므로, 이 프로젝트의 진짜 차별화는 "한계를 숨기지 않고 정량화해서 의사결정 프레임에 반영하는 태도"가 되어야 한다. 그것이 limitations.md와 의사결정 메모 형식을 설계에 넣은 이유.

---

## 8. 리뷰 반영 최종 결정사항 요약

1. 리포트 프레이밍: "니치 추천"이 아니라 **"진입 의사결정 프레임워크 + 과거 데이터 검증"** (R1, R5)
2. Q3 타깃을 "성공 예측"이 아니라 **"시장 안착 vs 조기 정체"로 명명** + 모집단 한계 명시 (R2)
3. 출시일 프록시 오차를 **직접 측정해 부록 수록** (R3)
4. 피처는 point-in-time 원칙, 시간 기반 split 강제 (R4)
5. **MVP = Q1+Q2+리포트**, Q3 확장, Q4 보너스로 단계별 완결 구조 (R7). *(v1.1 D4: MVP도 ~7주로 작지 않으므로, 그 안에 **최소 제출 가능 단위 = Q1 + 의사결정 리포트(약 4주)**를 별도로 정의해 어떤 시점에 멈춰도 완결된 포트폴리오가 되도록 한다.)*
6. 아마존 직접 크롤링(Playwright 포함) 배제 (6장)
7. 카테고리 2개, 대시보드 화면 3개로 스코프 하드캡 (R6, R10)

---

## 9. 2차 리뷰 반영 (D1~D10)

설계본(1~8장)을 채용 담당자 관점에서 한 번 더 점검해 도출한 잔여 결함과 반영 결과. §7 Red Team이 잡지 못한 **repo 경계·정의의 정량 확정·재현성 실무**에 집중.

| ID | 결함 | 반영 |
|----|------|------|
| D1 | repo 경계 모순 | §9.1 |
| D2 | Q3 타깃 잔여 순환성(임계값 누수·AND 결합) | §3.3, Stage 5, §7 R4 |
| D3 | 지표 산식 미확정 | Stage 3, §9.2 |
| D4 | MVP가 실제로 작지 않음(~7주) | §8.5 |
| D5 | LLM 비결정성 ↔ 재현성 충돌 | §2, Stage 4 |
| D6 | Q1 기회점수 외부 검증 부재 | Stage 3 |
| D7 | R3×R4 상호작용 미평가 | §7 |
| D8 | 데이터셋 라이선스 공개 적합성 | §9.3 |
| D9 | 데이터 버전 고정/manifest 부재 | §2, §9.3 |
| D10 | `my-design-system/` 정체 불명 | §9.4 |

### 9.1 repo 경계 결정 (D1)
이 저장소(`crm-uplift-analysis`)는 **기획·포트폴리오 메타 repo**로, 분석 설계 문서와 추천안을 담는다. 실제 분석 산출물(§5 구조: `src/`, `notebooks/`, `app/`, 대용량 `data/`)은 **별도 repo `amazon-seller-entry-analysis`**에 둔다. 이 repo의 `.gitignore`가 `amazon-seller-entry-analysis/`와 `data/`를 제외하는 것은 **의도된 분리**(대용량·중첩 repo를 메타 repo에 섞지 않음)임을 명문화한다.
- 열린 항목: 메타 repo 이름이 내용(아마존 셀러 분석)과 어긋남 → 추후 리네이밍 여부 결정 필요.

### 9.2 지표 산식표 (D3)
| 지표 | 산식 | 윈도우/정규화 |
|------|------|---------------|
| 수요 성장률 | 최근 12개월 니치 신규 리뷰 수 ÷ 직전 12개월 − 1 | 롤링, z-score |
| 경쟁 집중도(HHI) | Σ(상품별 니치 내 리뷰 점유율)² | 니치별, z-score |
| 신규 진입 속도 | 니치 월별 신규 상품(첫 리뷰 ASIN) 수의 추세 기울기 | 최근 24개월, z-score |
| 평점 갭 | 니치 평균 평점의 전체 니치 분포 내 백분위(낮을수록 기회) | z-score |
| 기회 점수 | 위 4개 z-score 가중합 | 가중치 민감도 분석 필수 |

### 9.3 데이터셋 라이선스·버전 고정 (D8, D9)
- Amazon Reviews 2023(McAuley Lab)의 라이선스·이용약관을 확인하고, 파생 산출물의 포트폴리오 공개 적합성을 README/limitations에 명시(연구용 데이터의 재배포 범위 주의).
- 재현성: HF 데이터셋 **revision/커밋 해시 핀** + 수집 산출물의 행 수·체크섬을 `data_quality_report.md`에 기록. 대용량 데이터는 추적하지 않되, 수집 스크립트와 manifest(소스·기간·필터·버전)로 재현 가능하게 한다.

### 9.4 열린 항목 (D10)
- `my-design-system/`(button·card·input·colors·typography·spacing HTML)은 본 분석 프로젝트·CRM 업리프트와 직접 연결고리가 없음 → 잔재인지 별개 작업인지 사용자 확인 후 분리/제거 결정.

---

## 10. 3차 리뷰 반영 (E1~E12)

D1~D10이 "정의·재현성·프레이밍"을 다뤘다면, 3차 리뷰는 **실제 데이터 스키마와 마주치는 실행 단계**의 결함에 집중. 데이터셋 카드(McAuley-Lab/Amazon-Reviews-2023: `language:en`, 리뷰 ~2023-09, 스크립트 기반)로 사실관계를 확인해 도출.

| ID | 결함 | 심각도 | 반영 위치 |
|----|------|--------|-----------|
| E1 | 분석 단위(asin vs parent_asin) 미정의 — 상품 수·HHI·출시일 왜곡 | 높음 | §3.2, Stage 1 |
| E2 | 12개월 타깃 × 2023-09 컷오프 우편절단(코호트 성숙도) | 높음 | §3.3, Stage 5 |
| E3 | 핵심 변수 price 대량 결측 위험 | 높음 | Stage 2 |
| E4 | 컷오프 경계가 수요 성장률·진입 속도를 하향 편향 | 중간 | Stage 3 |
| E5 | 니치 수가 적으면 z-score·백분위 불안정 | 중간 | Stage 3 |
| E6 | D2 재정의(상위 40%) 후 "심한 불균형→PR-AUC" 서술 stale | 중간 | Stage 5 |
| E7 | 배포 대시보드 런타임 데이터 부재(`data/` gitignore) | 중간 | Stage 7 |
| E8 | 환경 재현성(의존성 핀·Python 버전) 미명시 | 중간 | Stage 0 |
| E9 | 누수 방지가 수동 체크리스트뿐(자동 가드 테스트 부재) | 중간 | Stage 0 |
| E10 | Q3 유효 표본 작을 위험 → 과적합 | 낮음 | Stage 5 |
| E11 | 리뷰 원문 인용 시 PII 노출 | 낮음 | Stage 7 |
| E12 | Keepa 토큰 쿼터로 수집 범위 초과 가능 | 낮음 | Stage 6 |

**우선순위**: E1·E2·E3은 착수 즉시 막히는 종류 → 최상. E4·E7은 결과 신뢰도·전시 가능성에 직결. 나머지는 품질·재현성 보강.
